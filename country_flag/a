
cd ~/workspace/yash-pandey-criodo-ME_FE_BUILDOUT_XCOUNTRIES

git add .

git commit -m "ME_FE_BUILDOUT_XCOUNTRIES_MODULE_ONE"

git push -u origin master


# Ensure you have no pending commits

git status
